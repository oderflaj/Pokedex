import {StyleSheet} from 'react-native';

export const MessageStyle = new StyleSheet.create({
  errorMessageContainer: {
    borderWidth: 1,
  },
  informationMessageContainer: {
    borderWidth: 1,
  },
});
